server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:employeesservice
    driver-class-name: org.h2.Driver
    username: dbuser
    password: dbpass
  h2:
    console:
      enabled: true
      path: /console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect